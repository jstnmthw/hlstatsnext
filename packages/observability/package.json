{
  "name": "@repo/observability",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf node_modules dist .turbo",
    "lint": "eslint src/**/*.ts",
    "test": "vitest run --silent",
    "test:watch": "vitest --watch"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./prometheus": "./src/prometheus/index.ts",
    "./prisma": "./src/prisma/index.ts",
    "./types": "./src/types.ts"
  },
  "dependencies": {
    "@repo/database": "workspace:*"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^25.2.3",
    "eslint": "^9.39.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
