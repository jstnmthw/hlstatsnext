// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  output   = "../generated"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Models will be added during Phase 1.3: Database Design and Prisma Setup
// This follows the migration plan to start with foundation setup first 